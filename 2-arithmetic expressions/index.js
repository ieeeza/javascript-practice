let student = 20;

student = student + 1;
student = student - 1;
student = student * 2;
student = student / 2;

// & shows the remainder of a division
let extraStudents = students % 2;

// augmented assignment operator
students += 1;
students -= 1;
students *= 2;
students /= 2;

/* operator precedence
    1 - patenteses ( )
    2 - expoentes
    3 - multiplications & division
    4 - addintion & subtraction
*/

let result = 1 + 2 - 3 * (5 - 2);

console.log(result);